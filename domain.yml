version: "3.1"

intents:
  - greet
  - copy_content

responses:
  utter_greet:
    - text: "Hello! How can I help you today?"

actions:
  - action_copy_content

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
